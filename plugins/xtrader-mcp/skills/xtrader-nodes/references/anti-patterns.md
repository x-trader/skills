# Node Data Anti-Patterns

- Calling `get_node_details` as if it exists on AgentReady MCP v1.
- Setting node form data without inspecting required fields.
- Setting input values without checking type compatibility.
- Setting generic params without schema evidence.
- Setting array port types without validation.
- Guessing .NET runtime node display names without catalog search.
